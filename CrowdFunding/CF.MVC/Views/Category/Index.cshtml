@model CF.Models.MVC.Category.CategoryListViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@{
    foreach (var category in Model.Categories)
    {
        <div class="form-group">
    @category.Id @category.Name
           
        </div>
    }
}